name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  checks:
    name: Checks (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Syntax check
        run: python -m py_compile rdf_converter.py

      - name: CLI help smoke test
        run: python rdf_converter.py --help > /dev/null

      - name: Validate sample conversion
        shell: bash
        run: |
          printf '<http://example.org/s> <http://example.org/p> "x" .\n' > sample.nt
          python rdf_converter.py sample.nt sample.ttl
          python rdf_converter.py sample.ttl roundtrip.nt
          test -s sample.ttl
          test -s roundtrip.nt

  package:
    name: Build package
    runs-on: ubuntu-latest
    needs: checks
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install build tools
        run: python -m pip install --upgrade build twine

      - name: Build distributions
        run: python -m build

      - name: Check distributions
        run: python -m twine check dist/*

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: python-dist-ci
          path: dist/
          if-no-files-found: error
